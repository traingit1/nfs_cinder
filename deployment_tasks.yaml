# These tasks will be merged into deployment graph. Here you
# can specify new tasks for any roles, even built-in ones.

- id: cinder_nfs_role
  type: group
  role: [cinder_nfs_role]
  parameters:
    strategy:
      type: parallel

- id: cinder_nfs-deployment-puppet
  type: puppet
  role: [cinder_nfs_role]
  requires: [openstack-cinder]
  required_for: [deploy_end]
  parameters:
    puppet_manifest: cinder_nfs.pp
    puppet_modules: /etc/puppet/modules
    timeout: 500


#  version: 2.0.0              # tasks v2.0.0 is supporting task-based deployment
#   cross-depends:
#     - name: deploy_start
#   cross-depended-by:
#     - name: deploy_end

  parameters:
    puppet_manifest: "cinder_nfs.pp"
    puppet_modules: "."
    timeout: 3600

#- id: cinder_nfs-post-deployment-sh
#  version: 2.0.0
#  type: shell
#  role: [cinder_nfs_role]
#  requires: [post_deployment_start]
#  required_for: [post_deployment_end]
#  parameters:
#    cmd: echo post_deployment_task_executed > /tmp/post_deployment
#    retries: 3
#    interval: 20
#    timeout: 180

#- id: cinder_nfs-pre-deployment-sh
#  version: 2.0.0
#  type: shell
#  role: [cinder_nfs_role]
#  requires: [pre_deployment_start]
#  required_for: [pre_deployment_end]
#  parameters:
#    cmd: echo pre_deployment_task_executed > /tmp/pre_deployment
#    retries: 3
#    interval: 20
#    timeout: 180
